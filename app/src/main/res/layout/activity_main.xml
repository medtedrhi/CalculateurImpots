<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="20dp"
    android:gravity="center">

    <!-- Étiquette pour le nom -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/nom" />

    <!-- Champ de saisie pour le nom -->
    <EditText
        android:id="@+id/input_nom"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="@string/hint_nom"
        android:inputType="textPersonName"
        android:importantForAutofill="no" />

    <!-- Étiquette pour l'adresse -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:text="@string/adresse" />

    <!-- Champ de saisie pour l'adresse -->
    <EditText
        android:id="@+id/input_adresse"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="@string/hint_adresse"
        android:inputType="textPostalAddress"
        android:importantForAutofill="no" />

    <!-- Étiquette pour la surface -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:text="@string/surface_m" />

    <!-- Champ de saisie pour la surface -->
    <EditText
        android:id="@+id/input_surface"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="@string/hint_surface"
        android:inputType="numberDecimal"
        android:importantForAutofill="no" />

    <!-- Étiquette pour le nombre de pièces -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:text="@string/nombre_de_pi_ces" />

    <!-- Champ de saisie pour le nombre de pièces -->
    <EditText
        android:id="@+id/input_pieces"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="@string/hint_pieces"
        android:inputType="number"
        android:importantForAutofill="no" />

    <!-- Case à cocher pour la piscine -->
    <CheckBox
        android:id="@+id/checkbox_piscine"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:text="@string/piscine" />

    <!-- Bouton pour lancer le calcul -->
    <Button
        android:id="@+id/button_calcul"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:text="@string/calculer" />

    <!-- Affichage du résultat -->
    <TextView
        android:id="@+id/result"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="16sp"
        android:layout_marginTop="20dp"
        android:gravity="center" />

</LinearLayout>
